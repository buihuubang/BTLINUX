Copy file (Chỉ chọn 1 trong 3 file trong thư mục này) vào thư mục cần tạo hoặc đẩy lên github

Trong Linux: Ấn chuột phải, chọn open terminal here, gõ lệnh:
sudo bash AutoRes.bin 

Trong Windows:  Ấn chuột phải, chọn open cmd here, gõ lệnh:
AutoRes.cmd
